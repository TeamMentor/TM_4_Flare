dl.help.scroll
  span(ng-repeat = 'view in Views')
    dt.vertical-align
      p {{view.Title}}
    dd(ng-repeat = 'article in view.Articles')
      a(href='#'+'{{article.Id}}' ng-click='show_Doc(article)') {{article.Title}}
